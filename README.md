# Jerome's Dotfiles

```bash
$ git clone https://github.com/jeromegoodrich/dotfiles

$ cd dotfiles

$ ./link_files.rb

$ curl -fLo ~/.vim/autoload/plug.vim --create-dirs \
    https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim

$ vim PlugInstall
```
